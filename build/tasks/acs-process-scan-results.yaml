apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: acs-process-scan-results
spec:
  steps:
  - name: list-files
    image: centos
    resources: {}
    workingDir: /result-files
    script: >-
      #!/usr/bin/env bash

      ls -al
  workspaces:
  - name: result-files
    mountPath: /result-files
